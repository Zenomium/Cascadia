package graphical.controller.components;

public interface ErrorHandler {
	/**
	 * Allows to know if an error dialog box should be displayed if an error of placement has occurred
	 * @return
	 */
	boolean hasError();
}
